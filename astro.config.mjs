// @ts-check
import { defineConfig } from 'astro/config';

// https://astro.build/config
export default defineConfig({
	output: 'static',
	// Set these for production GitHub Pages deployments.
	// site: 'https://YOUR_GITHUB_USERNAME.github.io',
	// base: '/YOUR_REPO_NAME',
});
